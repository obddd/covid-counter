import React from 'react';
import './Card.style.scss';

const Card = (props) => {
    console.log(props)
    return (
        <div>
            <h1>Card</h1>
        </div>
    );
};

export default Card;